#!/bin/bash
#include <stdio.h>
#include <stddef.h>
#include "sort.h"

void bubble_sort(int *array, size_t size)
{
	for (size_t i = 0; i < size; i++ )
		int swapped = 0;
	{
		for (size_t v = 0; v < size - i - 1; v++ )
			if (array[j] > array[j+i])
			{
				swap(&array[j], &array[j+1]);
				swapped = 1;
			}
	if (swapped = 0)
	{
		break;
	}	
	}

}
